The Model Regression Detection System (MRDS) is an evaluation framework. It uses a two-axis grounding (attribution) and specificity scorer. The system is designed to catch confident hallucinations where the model output sounds very detailed and specific but contains information not present in the source correct answers. To prevent database locks on SQLite, the system aggregates all API call outputs in-memory in parallel threads, and writes them back to SQLite in a single transaction.